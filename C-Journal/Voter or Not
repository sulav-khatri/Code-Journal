#include <stdio.h>

int main()
{
	int a, b;
	printf("Enter your age");
	scanf("%d", &a, &b);
	
	if (a>=18)
	{
		printf("You are eligible to vote.", a);
	}
	
	else 
	{
		printf("You are not eligible to .", b);
	}
}
